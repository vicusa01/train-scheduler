@model CreateTicketModel
@{
    ViewBag.Title = "Create ticket";
}

<form asp-action="CreateTicket" asp-controller="Admin">
    <div asp-validation-summary="All" class="text-danger"></div>
    <div class="form-group">
        <label class="control-label" style=" margin-left: 39px;font-size:26px;">Destination</label>
        <select class="custom-select" aria-label="Select departure stop"
                asp-for="ScheduleId"
                asp-items="@Model.Schedules">
        </select>
    </div>
    <div class="form-group">
        <label class="control-label" style=" margin-left: 39px;font-size:26px;">User</label>
        <select class="custom-select" aria-label="Select departure stop"
                asp-for="UserId"
                asp-items="@(new SelectList(Model.Users, nameof(IdentityUser.Id), nameof(IdentityUser.Email)))">
        </select>
    </div>
    <div class="form-group">
        <label style=" margin-left: 39px;font-size:26px;" asp-for="Fio" class="control-label">FIO</label>
        <input type="text" asp-for="Fio" class="form-control" />
    </div>
    <div class="form-group">
        <input type="submit" value="Create" class="btn btn-outline-secondary" />
    </div>
</form>